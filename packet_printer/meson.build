# Top-level meson file for packet_printer sample

dev_app_name = 'packet_printer_app'
shared_include_files = [
	join_paths(meson.current_source_dir(), 'packet_printer_com.h'),
]

subdir('dev')
subdir('host')
